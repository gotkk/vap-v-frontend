const state = {
  mapstyle: ["dark-gray-vector", "streets-vector"],
};

const getters = {
  mapstyle: (state) => state.mapstyle,
};

const mutations = {};

const actions = {};

export default {
  state,
  getters,
  mutations,
  actions,
};
